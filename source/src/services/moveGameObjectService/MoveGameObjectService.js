class MoveGameObjectService {
    constructor(props) {
        const t = mapLayers
    }
}

MoveGameObjectService.propTypes = {
    mapLayers: PropTypes.object.isRequired,
};

export default MoveGameObjectService;
